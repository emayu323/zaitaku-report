// 最初の仮ルール：認証必須。運用開始後に役割ベースに強化してください。
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /patients/{patientId} {
      allow read, write: if request.auth != null;
      match /reports/{reportId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
