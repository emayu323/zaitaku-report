rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 認証必須（開発中は allow read, write: if true; でOK）
    match /patients/{pid}/reports/{doc} {
      allow read, write: if request.auth != null;
    }
  }
}
